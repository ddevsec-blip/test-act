name: test-work
on: workflow_dispatch
jobs:
  build: 
    runs-on: ubuntu-22.04
    steps:
      - name: build
        run: echo "building..."
  test:
    runs-on: ubuntu-22.04
    needs: build
    steps:
      - name: test
        run: echo "testing"
  deploy:
    runs-on: ubuntu-22.04
    needs: test
    steps:
      - name: deploy
        run: echo "deploying"
