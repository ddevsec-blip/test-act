name: if condition 
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: run if or
        if: github.actor == 'hemanth' || github.actor  == 'ddevsec-'
        run: echo "triggered"
